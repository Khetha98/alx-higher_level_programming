#!/usr/bin/node
// loops through an array and print strings

const listArray = ['C is fun', 'Python is cool', 'Javascript is amazing'];
for (let i = 0; i < listArray.length; i++) {
  console.log(listArray[i]);
}
